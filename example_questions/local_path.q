/**
 * Check for violations of local-path-consistency. Such a violation consists of a packet header that would take an accepting path out some interface of the originating node in the initial environment, but go out a different interface of the originating node in the delta environment.  
 */
local_path
