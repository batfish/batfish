/**
 * Check for violations of multipath-consistency. Such a violation consists of a packet header that under the provided environment could take multiple paths, at least one of which causes the packet to be accepted or successfully exit the network under test, and at least one of which causes the packet to be dropped.
 */
multipath
