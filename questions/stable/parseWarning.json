{
    "class": "org.batfish.question.initialization.ParseWarningQuestion",
    "differential": false,
    "aggregateDuplicates": "${aggregateDuplicates}",
    "instance": {
        "description": "Returns warnings that occurred when parsing the snapshot.",
        "instanceName": "parseWarning",
        "longDescription": "Return warnings such as failure to recognize certain lines and lack of support for certain features.",
        "tags": [
            "initialization"
        ],
        "variables": {
            "aggregateDuplicates": {
                "description": "Whether to aggregate duplicate results",
                "type": "boolean",
                "optional": true,
                "displayName": "Aggregate duplicates"
            }
        }
    }
}