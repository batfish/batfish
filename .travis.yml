matrix:
   include:
      - os: osx
        sudo: true
        install: ./.travis/install.sh
        script: ./.travis/build.sh
        language: generic
      - os: linux
        dist: trusty
        addons:
           apt:
              packages:
                 - oracle-java8-installer
        sudo: true
        install: ./.travis/install.sh
        script: ./.travis/build.sh
        language: generic
