routing-instances {
    test_vrf {
        instance-type vrf;
        vrf-import policy1;
        protocols {
            evpn {
                ip-prefix-routes {
                    import policy2;
                    vni 1000;
                }
            }
        }
    }
}
