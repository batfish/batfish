! Test config for new null statement handlers with M_NullLine mode
version 10.3(5)
hostname test-switch

! Power configuration (uses M_NullLine for arbitrary text)
power redundancy-mode ps-redundant

! Hardware configuration (uses M_NullLine for arbitrary text)
hardware ejector enable
no hardware module boot-order reverse
hardware access-list lou resource threshold 5
hardware profile multicast max-limit lpm-entries 2048

! High-availability policy (uses M_NullLine for arbitrary text)
ha-policy single-sup reload dual-sup switchover

! Keystore configuration (uses M_NullLine for arbitrary text)
keystore type default

! Telnet configuration (uses M_NullLine for arbitrary text)
telnet login-attempts 3

! VDC without ID clause
vdc combined-hostname

! VDC with ID clause (existing behavior)
vdc test-vdc id 1
  allocate interface Ethernet1/1
  limit-resource vlan minimum 16 maximum 4094
