package batfish.grammar.juniper.policy_options;

public enum ThenType {
   ACCEPT,
   COMMUNITY_ADD,
   COMMUNITY_DELETE,
   COMMUNITY_SET,
   LOCAL_PREF,
   METRIC,
   NEXT_HOP,
   NULL,
   REJECT
}
