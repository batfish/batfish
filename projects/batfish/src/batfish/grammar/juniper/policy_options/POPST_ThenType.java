package batfish.grammar.juniper.policy_options;

public enum POPST_ThenType {
   ACCEPT,
   AS_PATH_PREPEND,
   COMMUNITY,
   INSTALL_NEXT_HOP,
   LOCAL_PREF,
   METRIC,
   NEXT_HOP,
   NEXT_POLICY,
   NEXT_TERM,
   REJECT,
   
   NULL
}
